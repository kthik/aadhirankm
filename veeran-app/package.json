{
  "name": "veeran-android",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  },
  "scripts": {
    "build:web": "npm --prefix ../client run build",
    "sync": "npm run build:web && cap sync android",
    "apk:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "apk:release": "npm run sync && cd android && gradlew.bat assembleRelease",
    "open": "cap open android"
  },
  "description": "Android shell that packages the Veeran client (../client) as an installable app."
}
